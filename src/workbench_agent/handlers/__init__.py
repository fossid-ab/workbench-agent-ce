"""
Handlers Package - Handler implementations.

This package contains the handlers that use the
WorkbenchClient API architecture.

Import handlers from their modules (for example
``workbench_agent.handlers.scan``) rather than this package so unused
commands are not loaded at startup. ``main`` resolves handlers lazily.
"""
